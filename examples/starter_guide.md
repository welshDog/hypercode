# HyperCode Starter Guide

This guide demonstrates the core visual syntax of HyperCode. 
HyperCode uses a **spatial node-based** approach where logic is defined by connecting processing units.

## 1. Variables (Data Nodes)
Variables are "Data Nodes" that hold values. They flow into other nodes via wires.

```
┌─────────────┐
│ [📦] MyVar  │
├─────────────┤
│ Value: 42   │ ───🔵──▶ [Next Node]
└─────────────┘
```

## 2. Conditionals (If/Else)
Logic flows through "Control Nodes".

```
        ┌─────────────┐
──🔵──▶ │ [❓] Check  │
        ├─────────────┤
        │ Param: > 10 │
        ├─────────────┤
        │ True  ──────┼──🟢──▶ [Action A]
        │ False ──────┼──🟢──▶ [Action B]
        └─────────────┘
```

## 3. Loops (Repeat)
Loops are cyclic flows.

```
      ┌──────────────┐
┌─🟢──▶ [🔄] Loop    │
│     ├──────────────┤
│     │ Count: 5     │
│     ├──────────────┤
│     │ Body ────────┼──🟢──▶ [Do Work] ──┐
│     │ Done ────────┼──🟢──▶ [Continue]  │
│     └──────────────┘                    │
└─────────────────────────────────────────┘
```

## 4. Functions (Modules)
Functions are encapsulated "Black Boxes" with defined inputs and outputs.

```
┌──────────────────────┐
│ [𝑓] CalculateArea    │
├──────────────────────┤
│ Inputs:              │
│  • width  🔵 ───────▶│
│  • height 🔵 ───────▶│
├──────────────────────┤
│       [Logic]        │
├──────────────────────┤
│ Output:              │
│  ▶───────🔵 area     │
└──────────────────────┘
```

## 5. Comments (Notes)
Comments are "Sticky Notes" attached to nodes or groups, not inline text.

```
┌─────────────┐
│ [📦] UserID │ 
└──────┬──────┘
       │
    ┌──┴──────────────────┐
    │ 📝 TODO: Validated  │
    │ against DB schema   │
    └─────────────────────┘
```

## 6. Minimal Syntax (Text Equivalent)
For quick editing, the textual representation stays clean:

```hypercode
@data MyVar: 42
@check(MyVar > 10) -> {
    true: ActionA
    false: ActionB
}
```
